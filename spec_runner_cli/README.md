# spec_runner_cli

Rust CLI implementation for the canonical runner entrypoint.

This crate is intentionally focused on deterministic runner behavior for Data Contracts.
For full project context and operational workflows (`cargo xtask`), see the repository root `README.md`.
