# Repository Boundary Charter

`data-contracts-library` is the canonical home for reusable data-contract spec libraries and runner overlay manifests.

This repository must not redefine contract-spec schema authority.
