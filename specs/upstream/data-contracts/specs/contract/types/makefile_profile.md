# Makefile Subject Profile Contract (v1)

Makefile checks use JSON projection surfaces and library-backed predicates.

Projection rules:

- source text -> `value` string
- discovered targets -> JSON array in `context.targets`

Profile id: `makefile.generic/v1`.
